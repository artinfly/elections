# ==============================================================================
# Зависимости для запуска проекта (Продакшн)
# ==============================================================================
# Полный список пакетов, необходимых для работы приложения.
# Инструменты разработки (форматирование кода) исключены.
# Версии зафиксированы для воспроизводимости установки и работы с папкой wheels.

# --- Веб-фреймворк и его зависимости ---
Django==6.0.2
asgiref==3.11.1            # Асинхронный интерфейс (зависимость Django)
sqlparse==0.5.5            # Парсер SQL (зависимость Django)
tzdata==2025.3             # База данных часовых поясов (обязательно для Windows)

# --- Работа с Excel ---
openpyxl==3.1.5
et-xmlfile==2.0.0          # Библиотека для работы с XML внутри xlsx (зависимость)

# --- База данных ---
psycopg2-binary==2.9.11    # Драйвер PostgreSQL (бинарная версия без компиляции)

# --- Раздача статики ---
whitenoise==6.12.0         # Раздача статических файлов через Django