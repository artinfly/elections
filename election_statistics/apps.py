"""
Конфигурация приложения.

Описание:
    Содержит базовые настройки приложения, такие как его системное имя
    и человекочитаемое название для административной панели.
"""

from django.apps import AppConfig


class ElectionStatisticsConfig(AppConfig):
    """
    Конфигурация приложения.

    Описание:
        Определяет системное имя приложения и его отображаемое название.
        Дополнительные настройки (сигналы, проверка прав) здесь не требуются,
        так как логика приложения не зависит от событий старта проекта.
    """

    # Системное имя приложения (должно совпадать с названием папки).
    name = "election_statistics"

    # Человекочитаемое название приложения, отображаемое в админ-панели.
    verbose_name = "Учёт голосования"

    # Тип автоинкрементного первичного ключа для моделей приложения.
    # Дублирует настройку из settings.py для явности и переносимости.
    default_auto_field = "django.db.models.BigAutoField"
