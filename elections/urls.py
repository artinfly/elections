"""
Корневая карта маршрутов проекта.

Описание:
    Определяет точки входа в приложение. Все рабочие маршруты делегированы
    приложению election_statistics. Здесь подключается только административная панель.
"""

from django.contrib import admin
from django.urls import include, path

# Кастомизация заголовков админ-панели для соответствия корпоративному стилю.
admin.site.site_header = "Учёт голосования — Администрирование"
admin.site.site_title = "Учёт голосования — Портал администратора"
admin.site.index_title = "Управление данными"

urlpatterns = [
    # Маршруты административной панели.
    path("admin/", admin.site.urls),
    # Все основные маршруты приложения (страницы, API, загрузка, экспорт).
    # Пустая строка означает, что маршруты доступны от корня сайта.
    path("", include("election_statistics.urls")),
]
