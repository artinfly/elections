# elections

Учёт голосования работников. Принимает выгрузку Excel по сотрудникам, хранит её
в PostgreSQL, показывает статистику по способу голосования и явке. Способ и явку
можно проставлять вручную по каждому человеку или списком.

## Стек

Django 6, PostgreSQL, openpyxl, whitenoise. Фронтенд без сборки: Bootstrap лежит
файлом в `election_statistics/static/vendor/`.

## Развёртывание

```bash
python -m venv .venv
.venv/Scripts/python.exe -m pip install -r requirements.txt
.venv/Scripts/python.exe manage.py migrate
.venv/Scripts/python.exe manage.py createsuperuser
.venv/Scripts/python.exe manage.py setup_groups
.venv/Scripts/python.exe manage.py runserver
```

`requirements.txt` один на всё: и запуск приложения, и инструменты
форматирования (black, isort). Каждая строка файла соответствует одному
wheel-файлу в `wheels/` и наоборот.

### Установка без интернета

В `wheels/` лежат пакеты для офлайн-установки:

```bash
.venv/Scripts/python.exe -m pip install --no-index --find-links wheels -r requirements.txt
```

Python — строго 3.13.x: wheel'ы `black`, `psycopg2-binary` и `pytokens` собраны
под cp313-win_amd64. Остальные пакеты (`py3-none-any`) встанут на любую версию.

Готовую папку `.venv` переносить с другой машины нельзя — в ней записан
абсолютный путь к Python. Её создают заново на месте.

### Форматирование кода

```bash
.venv/Scripts/python.exe -m black .
.venv/Scripts/python.exe -m isort .
```

## Настройки

`elections/settings.py` — единственный файл настроек. Он лежит в репозитории и
правится под машину. При переносе на сервер меняются четыре вещи:

| Настройка | Значение на сервере |
| --- | --- |
| `SECRET_KEY` | собственный, не из репозитория |
| `DEBUG` | `False` |
| `ALLOWED_HOSTS` | адреса сервера |
| `DATABASES` | имя базы, пользователь, пароль, хост |

При `DEBUG = False` статику раздаёт whitenoise со сжатием и хешами в именах
файлов, поэтому её нужно предварительно собрать:

```bash
.venv/Scripts/python.exe manage.py collectstatic
```

При `DEBUG = True` работает обычное хранилище статики, собирать ничего не нужно.

Ответы сжимаются `GZipMiddleware`: страницы со списком работников ужимаются
примерно в десять раз. Размер файла, который принимается в память целиком, —
20 МБ (`FILE_UPLOAD_MAX_MEMORY_SIZE`), файлы крупнее пишутся во временный.

## Структура

```text
elections/                настройки проекта: settings.py, urls.py, wsgi.py

election_statistics/
├── models.py             схема базы: одна таблица сотрудников, коды способов
├── urls.py               маршруты
├── admin.py              админка: список сотрудников с галочками
├── views.py              страницы, фильтры, API простановки, экспорт
├── importers.py          чтение xlsx, простановка способа и явки
├── reports.py            стандартные отчёты и архивы по цехам (REPORT_MODES)
├── custom_reports.py     сводный отчёт по фильтрам (CUSTOM_COLUMNS)
├── helpers.py            общие утилиты чтения и оформления xlsx
│
├── management/commands/
│   ├── import_workshops.py   справочник цех - производство из внешней базы
│   └── setup_groups.py       создание группы operator
│
├── templates/
└── static/               стили, скрипты, Bootstrap (vendor/)

utils/
└── archiver.py           ReportArchiver: сборка книг openpyxl и байтов в zip

wheels/                   офлайн-пакеты: один wheel на строку requirements.txt
                          (в репозиторий не попадает, переносится отдельно)
```

## Страницы

| Адрес | Что показывает |
| --- | --- |
| `/` | способ голосования: плашки и таблица с простановкой |
| `/elections/` | явка: плашки, таблица, массовая отметка |
| `/upload/` | загрузка файла со списком работников |
| `/export/` | выгрузка: сводный отчёт по фильтрам, два архива по цехам, сводные таблицы (включая варианты без 19 округа), по производствам, полная таблица |
| `/login/`, `/logout/` | вход и выход |

Плашки считаются по текущему фильтру: сузил выборку по цеху — числа сверху
пересчитались по этому же цеху. После простановки способа или явки страница не
перезагружается, счётчики обновляются запросом с теми же фильтрами.

Плашка «УИК» кликабельна: открывает окно с распределением по участкам — сколько
человек приписано к каждому УИК, сколько из них проголосовало и какая явка.
Окно учитывает тот же фильтр, что и страница.

На странице «Выборы» два столбца: «Явка» — переключатель «проголосовал / не
голосовал», и «Где голосовал» — текст без кнопок. Место не выбирается руками:
при отметке явки туда подставляется способ, выбранный на первой странице, при
снятии отметки оно очищается. Если способ у человека не выбран, место остаётся
пустым.

## Формат файла

Строка заголовков ищется по названиям колонок, её положение в файле значения не
имеет. Берутся все колонки, кроме `№ п/п` и `CARD_ID`:

Подразделение, Таб№, Фамилия, Имя, Отчество, Должность, Категория,
Дата рождения, Регион, Город, Улица, Дом, УИК, Адрес УИК, Район, Округ.

Ключ — `Таб№`. Повторная загрузка обновляет существующие записи и **не стирает**
проставленные способ голосования и явку.

Загружается только этот файл. Способ голосования и явка проставляются вручную
на страницах сервиса.

Производство в этом файле не приходит. Его проставляет отдельная команда,
читающая справочник «цех - производство» из внешней базы:

```bash
.venv/Scripts/python.exe manage.py import_workshops --host <адрес> --dbname <база> --user <пользователь>
```

Пароль команда спрашивает при запуске. Она переписывает производство у всех
работников заново и в конце показывает, каких цехов нет в справочнике и каких
цехов из справочника нет у работников. Повторная загрузка файла работников
производство не стирает: этого поля нет среди колонок файла.

## Способ голосования

| Код | Что значит |
| --- | --- |
| `deg` | ДЭГ, дистанционное электронное голосование |
| `uik` | обычный участок |
| `uvz` | участок на предприятии |
| `u19` | участок 19 округа |
| пусто | не назначено |

Коды заданы в `models.py` и используются как значения фильтра и в API.

Полей два. `method` — «Способ», где человек планировал голосовать; заполняется
только на странице «Способ голосования» и повторной загрузкой файла не стирается.
`voted_method` — где он проголосовал; отдельно не редактируется, при отметке явки
копируется из `method`, при снятии очищается. План при этом не меняется.

Разделение нужно потому, что план можно поменять после того, как человек уже
проголосовал, и прошлая отметка от этого не поедет.

Плашки на странице «Способ голосования» считаются по плану, на странице
«Выборы» — по факту.

## Отметки регистрации

Два булевых поля: `mark_deg` — регистрация на ДЭГ, `mark_uvz` — регистрация на
УИК-УВЗ. Проставляются через API только при соответствующем выбранном способе:
отметить `mark_deg` можно лишь сотруднику с методом `deg`, иначе API вернёт
ошибку «способ не соответствует полю».

## Отметки «Открепился» и «Не пойдет»

Два дополнительных булевых поля сотрудника (`detached`, `not_going`), нужны для
сводного отчёта по фильтрам. В файле загрузки их нет — они проставляются вручную
в админке: оба поля выведены в список сотрудников колонками с галочками
(`list_editable`), отмечаются сразу у нескольких человек и сохраняются кнопкой
«Сохранить» внизу списка. Повторная загрузка файла работников эти отметки не
стирает, как и способ с явкой.

## Права доступа

| Роль | Права |
| --- | --- |
| суперпользователь | все страницы, включая загрузку |
| группа `operator` | загрузка файлов |
| группа `viewer` | только просмотр, без простановки |
| прочие | просмотр, простановка способа и явки, выгрузка |

Вход обязателен на всех страницах: в базе хранятся персональные данные —
ФИО, дата рождения и домашний адрес. Группа `operator` создаётся командой
`python manage.py setup_groups`.

## utils/archiver.py

`ReportArchiver` собирает несколько книг openpyxl и произвольных байтов в
zip-архив: `add_workbook`, `add_file`, `build_bytes`, `filenames`, `file_count`.
Повторное имя внутри архива без `overwrite=True` вызывает `ArchiveError`, поэтому
сборщик отчётов сам разводит совпадающие имена суффиксом. На нём построена
выгрузка отчётов по цехам.

## Выгрузка

| Что | Что внутри |
| --- | --- |
| сводный отчёт по фильтрам | один .xlsx по каждому работнику выборки: УИК, цех, таб.№, ФИО, округ и отметки по способам, «Не 19 округ», «Не пойдет»; внизу строка ИТОГО |
| архив по явке | zip, по одному .xlsx на цех: сколько проголосовало и кто не пришёл |
| архив по способу | тот же вид, но считаются выбравшие способ и список без выбора |
| сводная таблица | один .xlsx: строка на цех, ДЭГ / УИК / УИК-УВЗ / УИК-19, явка, процент |
| сводная таблица без 19 округа | та же таблица, но работники 19 округа не учитываются |
| разделение по производствам | один .xlsx: цеха сгруппированы по производствам, под каждым «Итого», внизу «Всего» |
| способы по производствам | то же, но вместо явки — распределение способов голосования |
| способы по производствам без 19 округа | та же таблица, но работники 19 округа не учитываются |
| полная таблица | один .xlsx: все колонки загрузки плюс план, явка и место |

Имя файла внутри архива — номер цеха из трёх цифр: `007.xlsx`, `097.xlsx`,
`130.xlsx`. Отчёт по цеху: заголовок с датой и номером, три числа и поимённый
список тех, у кого нужная отметка отсутствует. Оба архива строит одна функция,
различия режимов заданы в `REPORT_MODES` (`reports.py`).

В сводной таблице колонки способов считаются по **плану** (`method`), а
«Проголосовавшие» — по факту (`voted`), поэтому их суммы не совпадают. Поле
группировки — параметр `summary_table`, чтобы позже добавить разрез по
подразделениям без переписывания. Вариант без 19 округа дополнительно исключает
работников с округом `19`.

В разделении по производствам цеха без заполненного производства собраны в
последнюю группу «Без производства». Номер цеха дополняется нулями до трёх
цифр, как в именах файлов архива. Вариант «без 19 округа» строит та же функция
`production_method_table` с параметром `exclude_u19=True`.

### Сводный отчёт по фильтрам

Форма-конструктор стоит на странице `/export/` над остальными отчётами.
Фильтры: производство, способ (план), цех, где голосовал, округ (все / пусто /
19 / 20 / 21 / 20+21) и УИК. Кнопка «Сформировать» отправляет фильтры GET-ом
на `/export/custom/` и сразу отдаёт готовый .xlsx.

Таблица повторяет бумажный макет: пять ведущих колонок (номер УИК, цех, таб.№,
ФИО, округ) и группы колонок «ДЭГ», «На участке», «На участке УВЗ» с
подколонками «Планирует» и «Проголосовал», группа «Не 19 округ» с подколонками
«Планирует», «Открепился», «Проголосовал» и одиночная колонка «Не пойдет».
В ячейке ставится 1, если условие выполнено:

- «Планирует» — у сотрудника выбран соответствующий способ (`method`);
- «Проголосовал» — отмечена явка и `voted_method` равен способу;
- «Не 19 округ» — колонки считаются только для работников не из 19 округа;
- «Открепился» и «Не пойдет» — ручные отметки (см. раздел про них выше).

Внизу строка ИТОГО с суммами по каждой колонке. Вся семантика колонок собрана
в одном списке `CUSTOM_COLUMNS` (`custom_reports.py`) — если смысл колонки
изменится, правится только он.